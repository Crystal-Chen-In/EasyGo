EasyGo 美团周末出行规划 Agent 系统设计文档

一、 引言与系统架构概述

EasyGo 是一款面向本地短途（4-6小时）出行的智能体助理系统。系统旨在通过轻量级纯前
端技术栈，无缝集成主流大语言模型（LLM）API，解决用户周末出行中需求模糊、信息分散
、预订繁琐的痛点。系统基于“规划（Planning）— 检索（Retrieval）— 执行（Execution）”三层智能架构开发，并整合高德地图与心知天气 API，具备多轮意图澄清、自动化行程预订及无障碍人机交互的能力。
# EasyGo 美团周末出行规划 Agent 系统设计文档

## 目录

- [引言与系统架构概述](#引言与系统架构概述)
- [核心设计理念：长辈与视力障碍友好模式](#核心设计理念长辈与视力障碍友好模式)
	- [视觉动画优化](#视觉动画优化)
	- [全页Emoji标识](#全页emoji标识)
	- [TTS语音播报](#tts语音播报)
- [Planning（规划）策略](#planning规划策略)
	- [多轮意图抽取与澄清](#多轮意图抽取与澄清)
	- [双通道并发检索筛选](#双通道并发检索筛选)
	- [AI时序编排与降级兜底](#ai时序编排与降级兜底)
- [工具调用链路](#工具调用链路)
- [系统异常处理与容灾机制](#系统异常处理与容灾机制)

## 引言与系统架构概述

EasyGo 是一款面向本地短途（4-6 小时）出行的智能体助理系统。系统旨在通过轻量级纯前端
技术栈，无缝集成主流大语言模型（LLM）API，解决用户周末出行中需求模糊、信息分散、预
订繁琐的痛点。系统基于“规划（Planning）— 检索（Retrieval）— 执行（Execution）”三
层智能架构开发，并整合高德地图与心知天气 API，具备多轮意图澄清、自动化行程预订及无障
碍人机交互的能力。

## 核心设计理念：长辈与视力障碍友好模式

本系统将“无障碍友好”作为底层核心设计原则。系统全程适配视障、老年及视力偏弱用户使用
场景，通过视觉优化、图标适配、语音辅助全方位降低操作门槛，实现全场景无障碍适配。

### 视觉动画优化

字号整体放大 1.5~2 倍，适配老花、弱视人群；全部按钮、卡片增设悬浮缩放与阴影微动效，
点击触感反馈清晰。

### 全页Emoji标识

页面各处文字配套对应表情图标，所有功能按钮标配专属 Emoji，用图标辅助辨识控件，进一步
助力视障、视力偏弱用户快速区分功能。

### TTS语音播报

内置端侧语音引擎，页面控件自带语音锚点；点击行程、天气、预订等卡片时，即时打断原有播
报，异步朗读组件内容与用途。

## Planning（规划）策略

EasyGo 的 Planning 策略采用“意图抽取与澄清”加“三段式时序编排”双阶段流水线设计。具体
规划执行细则如下：

### 多轮意图抽取与澄清

系统解析用户自然语言需求，输出含出行人群、人数、范围、时长、特殊需求等参数的结构化数
据，其中出行人群类别、成人人数为必填项。若关键信息缺失，将会通过追问来补全信息；连
续两轮追问未完善时，系统自动填充安全默认值，避免流程卡死。

### 双通道并发检索筛选

意图确认后，系统并行检索周边活动与餐饮资源，生成优质候选集。针对老人、儿童同行场景，
系统自动过滤筛选，优先推荐配备无障碍通道、电梯、儿童餐等适配设施的商户，贴合特殊出
行需求。

### AI时序编排与降级兜底

AI 基于获取的参数与候选资源，生成「活动—餐饮—活动」三段式时序行程，精准规划各站点时长、
到达时间及人均开销。若网络或大模型调用异常，系统将触发本地规则兜底机制，通过固定交通
缓冲时间算法，自动生成可用行程，保障规划功能全天候可用。

## 工具调用链路

系统通过严格定义的工具链交互协议，在纯浏览器沙箱内协同完成全套逻辑。核心工具链规范如下：

| 工具名称 | 输入参数 | 输出参数/状态 | 说明 |
|---|---|---|---|
| extract_intent | 用户自然语言、API 密钥、追问轮数 | 结构化 Intent JSON、澄清问句、降级标识 | 解析需求，必要时挂起并启动语音追问，生成结构化配置 |
| search_activities | Intent JSON、位置经纬度、检索半径 | 符合空间与无障碍约束的活动候选数组 | 检索半径内活动地点，优先筛选适宜长辈、含无障碍设施的景点 |
| search_restaurants | Intent JSON、位置经纬度、检索半径 | 满足人数与特殊偏好的餐饮候选数组 | 并发过滤餐饮商户，获取价格与等位状态，过滤健康与无障碍属性 |
| plan_itinerary | Intent JSON、活动/餐饮候选、大模型密钥 | 时序化三段式行程单 JSON (stops) | 将候选集编排为有时序的最终方案，生成适合 TTS 语音朗读的文本摘要 |
| excute_booking | 行程节点 (Stops)、Intent、进度回调 | 预约执行详情数组、异常列表、总结文字 | 针对收费景点购票、餐厅留位，并异步语音播报与推送当前预订进度 |
| share_plan | 行程节点、Intent、预订结果 | 微信格式、TTS 格式、紧凑文本、二维码 | 生成多格式社交文案，TTS 专用格式针对听觉进行重音与停顿优化 |
| get_weather | 当前定位城市名称、心知天气密钥 | 天气现象、实时温度、图示 URL、5h 预测 | 获取实况天气并更新 DOM；友好模式下直接展示大字号气象与语音播报 |

## 系统异常处理与容灾机制

### 1. LLM API 请求超时与多级备用 Key 熔断链

使用 `AbortSignal.timeout` 对网络通信实施强管控：意图抽取限制超时为 8 秒，行程规划限制超
时为 15 秒。网络请求采用“链式调用熔断器”逻辑：若优先使用的智谱 GLM-4 接口异常，立即
捕获并转投 DeepSeek API；若其同样失败，则顺延转至 LongCat-Plus 接口。全数断开时，主动
激活离线规则引擎，并告知用户 AI 服务暂时繁忙，已用智能规则快速生成可用方案。

### 2. 预订任务异步重试容灾

在 `excute_booking.js` 的预订链路中，对购票与餐厅定位等事务增加内部重试计数（`retry = 1`）
。首次失败时延时 300 毫秒后二次尝试；若再次失败，该步骤标记为 `failed`，但不熔断整
个进程，并在执行总结中通过语音温和提示，确保规划逻辑的连续性。

### 3. 地图与天气 API 的边缘失效兜底

当心知天气 API 请求错误或超限时，气温面板显示“天气信息不可用”，天气图标自动回退至高
对比度的大尺寸默认 Emoji（如 🌈），防止版面错乱影响老人视线；高德地图 API 在脚本无
法加载或定位权限被拒时，预览图卡片转为静态格子状 SVG 辅助底图呈现。

---

*文档由工作区内的原始 Word 文档转换而来，内容保留原貌并做格式化以便阅读与版本管理。*
